<%= form_with(model: user_registration) do |form| %>
  <% if user_registration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_registration.errors.count, "error") %> prohibited this user_registration from being saved:</h2>

      <ul>
        <% user_registration.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :user_name %>
    <%= form.text_field :user_name %>
  </div>

  <div class="field">
    <%= form.label :password %>
    <%= form.text_field :password %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
